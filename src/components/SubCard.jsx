import React from 'react';

const SubCard = ({subItem}) => {
    const {subcategory_Name} = subItem
    return (
        <div>
            ffffffffff: {subcategory_Name}
        </div>
    );
};

export default SubCard;